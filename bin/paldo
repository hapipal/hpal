#!/usr/bin/env node

const Paldo = require('..');

Promise.resolve()
    .then(() => Paldo.start({ args: process.argv }))
    .then(() => process.exit(0))
    .catch((err) => {

        console.error(err.stack);
        process.exit(1);
    });
